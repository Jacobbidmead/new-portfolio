import { ReactComponent as Icon } from "../icons/git.svg";
import "../App.css";

const GitIcon = () => {
  return (
    <div>
      <Icon className="git-icon" />
    </div>
  );
};

export default GitIcon;
