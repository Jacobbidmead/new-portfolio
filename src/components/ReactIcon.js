import { ReactComponent as Icon } from "../icons/react.svg";

const ReactIconComponent = () => {
  return (
    <div style={{ paddingTop: "20px" }}>
      <Icon />
    </div>
  );
};

export default ReactIconComponent;
